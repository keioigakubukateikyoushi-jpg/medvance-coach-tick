# medvance-coach-tick

Medvance のLINEコーチが、学習計画に沿って自動でメッセージを送るための「15分ごとの起動」だけを置くリポジトリです。

- `tick.yml`: 15分ごとに送信処理を呼び出す（GitHubの混雑で数分〜15分ほど遅れることがある）
- `keepalive.yml`: 公開リポの定期実行が60日で止まらないよう、月1回空コミット
- 秘密情報は置いていません。呼び出し先は GitHub の OIDC トークンで、このリポの main からの実行だけを受け付けます
